// next.config.js (older syntax, or if you prefer .js)

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['d3i6fh83elv35t.cloudfront.net', /* 'other.domain.com' */],
  },
  // ... any other configurations you have
};

module.exports = nextConfig;